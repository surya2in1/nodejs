// Node uses commonjs and every file is module by default in node application
// Module - Encapsulate the code
const fname = "Surya";
const sname = "Peter";

function sayHello(name) {
  console.log(`Hello there ${name}`);
}

sayHello(fname);
sayHello(sname);
